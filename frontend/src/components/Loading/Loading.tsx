const Loading = () => {
  return (
    <main className='flex flex-col container items-center justify-center mx-auto h-screen'>
      <h1 className='text-5xl'>Loading...</h1>
    </main>
  );
};

export default Loading;
